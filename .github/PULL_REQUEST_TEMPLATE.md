<!--
Thanks for contributing to The Maze Game! Please fill this in so reviews go fast.
Keep PRs focused — one concern per PR where possible.
-->

## Summary

<!-- What does this PR do, and why? Link the issue it closes. -->

Closes #

## Area

<!-- Check all that apply. -->

- [ ] Frontend (game UI — `src/`, `index.html`)
- [ ] Backend API (`server/`, `api/`)
- [ ] Accounts / auth (JWT, bcrypt)
- [ ] `mazeforge` Python library (`src/python/`)
- [ ] Docker / CI (`Dockerfile*`, `.github/workflows/`)
- [ ] Docs (`*.md`, Swagger)
- [ ] Other

## Type of change

- [ ] 🐛 Bug fix (non-breaking change that fixes an issue)
- [ ] ✨ New feature (non-breaking change that adds functionality)
- [ ] 💥 Breaking change (fix or feature that changes existing behavior)
- [ ] 🧹 Refactor / chore (no functional change)
- [ ] 📝 Docs only

## How was this tested?

<!-- Commands you ran + what you observed. Screenshots/GIFs for UI changes. -->

```bash
# e.g.
npm test
npm run format:check
pytest src/python
```

## Checklist

- [ ] My code follows the existing style (`npm run format` / Prettier passes).
- [ ] I added or updated tests where it makes sense.
- [ ] All tests pass locally (`npm test` and, if Python changed, `pytest src/python`).
- [ ] I updated the relevant docs (README / API_DOCUMENTATION / module READMEs).
- [ ] No secrets, `.env` files, or build artifacts are committed.
- [ ] The branch is up to date with `master`.

## Screenshots / notes

<!-- Optional: before/after, edge cases, follow-ups, anything reviewers should know. -->
